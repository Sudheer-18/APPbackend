const express = require('express');
const router = express.Router();
const { generateQuestion } = require('../Controller/GeminiController');

router.post('/generate-question', generateQuestion);

module.exports = router;
